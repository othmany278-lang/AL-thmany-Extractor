# اختبار WhatsApp E2E الآلي — GitHub فقط

هذا الاختبار مخصص للتحقق من **آلية التطبيق** بدون تنزيل APK على هاتف المستخدم في كل تعديل.

## ما الذي يختبره؟

- اختيار الحزمة `com.whatsapp` فعليًا.
- فتح روابط `chat.whatsapp.com` واحدًا تلو الآخر.
- قراءة واجهة تطبيق مستقل عبر Accessibility الحقيقي للتطبيق.
- الضغط على `Join group`.
- الضغط على `Request to join`.
- تمييز `Join community` والضغط على `Continue` عند ظهوره.
- التحقق من نجاح الانضمام من شاشة دردشة فعلية داخل المحاكي.
- التحقق من حالة `Request pending`.
- حالات: رابط غير صالح، عضو بالفعل، المجموعة ممتلئة، تمت الإزالة، حد الحساب.
- مسار `X/Close` ومسار `Back` الاحتياطي.
- حارس يمنع الانتقال للرابط التالي إذا لم تُغلق شاشة الرابط السابق.
- وضع `SCAN_ONLY` للتأكد من القراءة بدون تنفيذ الانضمام.

## ما الذي لا يثبته؟

هذا ليس WhatsApp الحقيقي ولا يحتوي حسابًا أو OTP. لذلك يثبت أن **محرك AL-thmany نفسه** يفتح الحزمة، يقرأ العقد، يضغط، يتحقق، يغلق وينتقل بصورة صحيحة. أي تغيير مستقبلي في واجهة WhatsApp الحقيقية قد يحتاج تحديث قواعد القراءة.

## النتيجة في Actions

سيظهر فحص مستقل باسم:

`WhatsApp Join E2E Test`

عند النجاح تظهر رسالة:

`E2E PASS: JOIN_ONLY + SCAN_ONLY + Join + Request + Community + Continue + X/Back + next-link progression`

وعند الفشل يتم رفع Artifact باسم `WhatsApp-E2E-Report` يحوي نتيجة JUnit وLogcat ولقطة شاشة وAccessibility dump.
