# AL-thmany Extractor 2.14.2 — Group Sync Runtime Fix

هذا الإصدار يعالج العطل الظاهر في فيديوهَي التشغيل حيث كان الاستخراج والنشر يفتحان واتساب ثم يبقيان داخل قائمة الدردشات بدون الدخول إلى القروب الصحيح.

## إصلاح المزامنة
- تفعيل فلتر **المجموعات** الحقيقي في WhatsApp قبل حفظ GroupRecord.
- قراءة صفوف قائمة الدردشات فقط بدل أي عنصر Clickable في الشاشة.
- استبعاد عناصر الواجهة: الكل، غير مقروءة، المفضلة، المجموعات، المؤرشفة، Meta AI، بطاقات مزامنة جهات الاتصال والتنقل الرئيسي.
- القروبات المكتشفة الجديدة تبدأ `selected=0` ولا تبدأ حملة استخراج/نشر قبل اختيار المستخدم.
- عند نجاح فلتر المجموعات يسجل القروب كـ `verifiedGroup` لتجنب تحقق معلومات القروب غير الضروري أثناء النشر.

## تنظيف ذاكرة 2.14 القديمة
- SQLite DB v11.
- إضافة `sync_generation` و `stale`.
- عند الترقية يتم إخفاء وإلغاء تحديد الاكتشافات القديمة التي قد تحتوي عناصر واجهة خاطئة.
- بعد كل مزامنة، السجلات المكتشفة التي لم تظهر في الجيل الجديد تصبح Stale وغير قابلة للنشر، مع إبقاء السجل التاريخي بدل حذفه.
- إذا لم تُقرأ أي مجموعة أثناء المزامنة، لا يتم إفساد قاعدة القروبات الحالية.

## إصلاح فتح القروبات
- فلتر المجموعات أولًا.
- مطابقة صف القروب المرئي بدون Search.
- تمرير للأمام والخلف داخل **قائمة الدردشات نفسها**.
- Gesture tap fallback عند رفض `ACTION_CLICK`.
- التحقق من Header القروب من `text` أو `contentDescription`.
- إذا لم يثبت فتح القروب، يعود المحرك إلى القائمة قبل أي Scroll إضافي.
- Search يبقى الحل الأخير فقط.

## الاستخراج والنشر
- الاستخراج لا يبدأ قراءة/تمرير الرسائل إلا بعد إثبات فتح القروب الصحيح.
- النشر يستخدم نفس GroupRecord وقاعدة القروبات المزامنة.
- النشر يستبعد القروبات غير النشطة أو غير القابلة للنشر أو Community Parent.
- تبقى حماية `UNCERTAIN` ومنع إعادة الإرسال العمياء كما هي.
